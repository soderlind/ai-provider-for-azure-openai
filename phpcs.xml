<?xml version="1.0"?>
<ruleset name="AI Provider for Azure OpenAI">
	<description>WordPress Coding Standards for AI Provider for Azure OpenAI.</description>

	<file>./</file>

	<exclude-pattern>vendor/*</exclude-pattern>
	<exclude-pattern>node_modules/*</exclude-pattern>
	<exclude-pattern>tests/*</exclude-pattern>
	<exclude-pattern>build/*</exclude-pattern>
	<exclude-pattern>*.js</exclude-pattern>

	<rule ref="WordPress"/>
	<rule ref="WordPress-Docs"/>
	<rule ref="WordPress-Extra"/>

	<!-- PSR-4 autoloading: suppress WordPress filename conventions -->
	<rule ref="WordPress.Files.FileName.NotHyphenatedLowercase">
		<exclude-pattern>src/*</exclude-pattern>
	</rule>
	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>src/*</exclude-pattern>
	</rule>

	<rule ref="PHPCompatibility"/>
	<config name="testVersion" value="7.4-"/>

	<!-- Set minimum supported WordPress version -->
	<config name="minimum_supported_wp_version" value="7.0"/>

	<!-- Text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="ai-provider-for-azure-openai"/>
			</property>
		</properties>
	</rule>

	<!-- Prefixes -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="azure_openai"/>
				<element value="WordPress\AzureOpenAiAiProvider"/>
			</property>
		</properties>
	</rule>
</ruleset>
